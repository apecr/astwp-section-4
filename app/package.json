{
  "name": "section4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "python -m unittest discover -s test -p test_*.py",
    "install": "pip install -r app/requirements.txt",
    "start": "python app/app.py",
    "sonar": "sonar-scanner",
    "coverage": "coverage run --source app -m unittest discover -s test && coverage report -m --fail-under 80 && coverage xml -o coverage-reports/coverage.xml"
  },
  "author": "",
  "license": "ISC"
}
